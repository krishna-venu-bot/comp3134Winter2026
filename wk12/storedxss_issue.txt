The application reads data from a file and directly outputs it to the browser without sanitization. This allows malicious scripts to execute automatically whenever the page loads, resulting in a stored XSS vulnerability.
